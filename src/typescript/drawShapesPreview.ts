import type { ToolState } from "../redux/slices/tools"

type functionProps = {
    ctxPreview: CanvasRenderingContext2D,
    tool: ToolState
}


const drawShapesPreview = ({tool}:functionProps) => {
}

export default drawShapesPreview